# Visualization Tests

Tests in this folder cover Folium map helpers.

They verify that map generation handles empty GeoDataFrames and representative
inputs without crashing, and that map outputs are written through the atomic HTML
helper rather than directly saving over existing artifacts.
